/*
A Controller used to setup intial behavior for the `$scope`, it can hold buesness and presintation lodic
and is a intermitery between the View (HTML) and the Model (Database).
*/

// Create module "app" with no dependancies
angular.module('app', []);
